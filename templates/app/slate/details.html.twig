{% extends 'app/index.html.twig' %}

{% block body %}
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="sm:flex sm:justify-between sm:items-center mb-3">
            <a class="text-sm font-medium text-violet-500 hover:text-violet-600 dark:hover:text-violet-400" href="{{ path('app_slate') }}">
                &larr; Retour
            </a>
            <div class="grid grid-flow-col sm:auto-cols-max justify-start sm:justify-end gap-2">
                <button class="btn bg-gray-900 text-gray-100 hover:bg-gray-800 dark:bg-gray-100 dark:text-gray-800 dark:hover:bg-white" data-modal-toggle="add-slate-modal">
                    + Ouvrir une ardoise
                </button>
            </div>
        </div>

        <header class="mb-4">
            <h1 class="text-2xl md:text-3xl text-gray-800 font-bold dark:text-gray-100">Ardoise du {{ event.date|format_date('full') }}</h1>
        </header>

        <div class="bg-white dark:bg-gray-800 shadow-xs rounded-xl">
            <header class="px-5 py-4">
                <h2 class="font-semibold text-gray-800 dark:text-gray-100">Ardoise totale <span class="text-gray-400 font-medium">{{ membersData|reduce((carry, data) => carry + data.total, 0)|number_format(2) }} €</span></h2>
            </header>
            <div class="overflow-x-auto">
                <table class="table-auto w-full">
                    <thead class="text-xs font-semibold uppercase text-gray-500 dark:text-gray-400 bg-gray-50 dark:bg-gray-700 border-t border-b border-gray-100 dark:border-gray-600">
                    <tr>
                        <th class="px-2 first:pl-5 last:pr-5 py-3 whitespace-nowrap">
                            <div class="font-semibold text-left">Numéro d'adhérent</div>
                        </th>
                        <th class="px-2 first:pl-5 last:pr-5 py-3 whitespace-nowrap">
                            <div class="font-semibold text-left">Total dépensé</div>
                        </th>
                        <th class="px-2 first:pl-5 last:pr-5 py-3 whitespace-nowrap">
                            <div class="font-semibold text-left">Actions</div>
                        </th>
                    </tr>
                    </thead>
                    <tbody class="text-sm divide-y divide-gray-100 dark:divide-gray-600">
                    {% if membersData is empty %}
                        <tr>
                            <td class="px-2 first:pl-5 last:pr-5 py-3" colspan="3">
                                <p class="text-gray-500 dark:text-gray-400">Aucune consommation enregistrée pour cet événement.</p>
                            </td>
                        </tr>
                    {% else %}
                        {% for memberNumber, data in membersData %}
                            <tr>
                                <td class="px-2 first:pl-5 last:pr-5 py-3 whitespace-nowrap">
                                    <div class="text-left">{{ memberNumber }}</div>
                                </td>
                                <td class="px-2 first:pl-5 last:pr-5 py-3 whitespace-nowrap">
                                    <div class="text-left">{{ data.total|number_format(2) }} €</div>
                                </td>
                                <td class="px-2 first:pl-5 last:pr-5 py-3 whitespace-nowrap">
                                    <a href="{{ path('app_slate_member_details', {'event': event.id, 'memberNumber': memberNumber}) }}"
                                       class="text-indigo-500 dark:text-indigo-400 hover:text-indigo-600 dark:hover:text-indigo-300">Détails</a>
                                </td>
                            </tr>
                        {% endfor %}
                    {% endif %}
                    </tbody>
                </table>
            </div>
        </div>

        {# Modal pour ajouter une consommation #}
        <div id="add-slate-modal" class="fixed inset-0 bg-gray-600 bg-opacity-50 hidden flex items-center justify-center z-50">
            <div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-lg max-w-lg w-full">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-semibold text-gray-800 dark:text-gray-100">Ouvrir une ardoise</h2>
                    <button data-modal-close="add-slate-modal" class="text-gray-600 dark:text-gray-300 hover:text-gray-800 dark:hover:text-gray-100">&times;</button>
                </div>
                <form id="add-slate-form" action="{{ path('app_slate_consumption_add', {'event': event.id}) }}" method="post" data-ajax-form data-csrf="{{ csrf_token('add_consumption') }}">
                    {{ form_start(form) }}
                    <input type="hidden" name="_token" value="{{ csrf_token('add_consumption') }}" class="csrf-token">
                    <div class="grid grid-cols-1 gap-6">
                        <div>
                            {{ form_label(form.member, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300'}}) }}
                            {{ form_widget(form.member, {'attr': {'class': 'mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 shadow-sm'}}) }}
                            {{ form_errors(form.member) }}
                        </div>
                        <div>
                            {{ form_label(form.consumable, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300'}}) }}
                            {{ form_widget(form.consumable, {'attr': {'class': 'mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 shadow-sm'}}) }}
                            {{ form_errors(form.consumable) }}
                        </div>
                        <div>
                            {{ form_label(form.quantity, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300'}}) }}
                            {{ form_widget(form.quantity, {'attr': {'class': 'mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 shadow-sm'}}) }}
                            {{ form_errors(form.quantity) }}
                        </div>
                    </div>
                    <div class="mt-6 flex justify-end">
                        <button type="button" data-modal-close="add-slate-modal" class="text-gray-600 dark:text-gray-300 px-4 py-2 hover:text-gray-800 dark:hover:text-gray-100">Annuler</button>
                        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 dark:hover:bg-blue-500 ml-4">Ajouter</button>
                    </div>
                    {{ form_end(form) }}
                </form>
            </div>
        </div>
    </div>

    <div id="toast-container" class="fixed top-4 right-4 z-50 space-y-2"></div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('add-slate') }}
    <script data-turbo-eval="false">
        console.log('details.html.twig scripts initialisés');
    </script>
{% endblock %}
